import React from 'react';

import './Header.scss'

function Header(props) {
    return (
        <div className='header' >
            This is the header
        </div>
    );
}

export default Header;